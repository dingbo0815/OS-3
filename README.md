# OS-3
在最基礎的 Blink 階段，重點在於確認硬體腳位與電路導通，使用 sleep_ms 雖然直觀，卻會讓 CPU 在等待時空轉，無法處理其他事務。到了 LED 封裝階段，開始有了模組化意識，透過定義 LED_PAD 讓程式碼具備更好的可讀性與維護性，最關鍵的在於 LEDExt (FreeRTOS) 的實作。引進作業系統後，利用 vTaskDelay 取代傳統延遲，讓系統能在閃燈的空檔切換去執行其他計算或讀取感測器。這種非阻塞式的架構，雖然增加了初始配置的複雜度但它是開發複雜系統不可或缺的技能。
